package app.grapheneos.gmscompat

/**
 * Use this with a NavController to navigate. If arguments are desired, it would be better to
 * use https://developer.android.com/guide/navigation/design/kotlin-dsl which requires
 * "kotlinx_serialization_core" and "kotlinx_serialization_json" imports.
 */
object NavRoutes {
    const val Main = "main"
}